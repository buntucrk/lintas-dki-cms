import React from "react";

export default function ArticleCard({ title, excerpt }: { title: string; excerpt?: string }) {
  return (
    <article>
      <h3>{title}</h3>
      <p>{excerpt}</p>
    </article>
  );
}
